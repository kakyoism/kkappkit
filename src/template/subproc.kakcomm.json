{
  "_version": 1,
  "protocol": "subprocess",
  "submit": {
    "sender": "${kakctrl}.action.submit",
    "topic": "poetry run python ${cli}",
    "payload": "${kakbind}",
    "response": "",
    "cwd": "${kak}.backend.root.${platform}"
  }
}